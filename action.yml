name: 'DuckDB setup'
description: 'Install and setup DuckDB'
inputs:
  version:  # version of duckDB
    description: 'The version to install'
    required: false
    default: 'v0.7.1'
runs:
  using: 'node16'
  main: 'dist/index.js'